备忘录模式是一种行为设计模式，用于保存对象的某个历史状态，以便之后可以恢复到这个状态，而不会暴露对象的内部细节。
你可以把它想象成游戏存档、编辑器的撤销功能（Ctrl + Z），甚至是数据库的事务回滚。

备忘录模式由 三个主要角色 组成：
1️⃣ 发起者（Originator）
负责创建和恢复状态。
生成一个备忘录对象（Memento），用于存储当前状态。

2️⃣ 备忘录（Memento）
负责存储发起者的内部状态。
不允许外部直接修改其状态（只能由发起者访问）。

3️⃣ 负责人（Caretaker）
负责存储多个备忘录对象，管理历史状态。
不能修改备忘录的内容，只能存取备忘录。


