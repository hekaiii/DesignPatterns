表达式在解释器模式中就是一棵表达式语法树（AST），结构上就是二叉树，解释过程就是对这棵树进行递归遍历和求值。

在解释器模式中，操作符（如加法、减法）和操作数（如变量、常量）都实现了相同的抽象接口 Expression，
每个操作符节点会递归调用它的子表达式的 interpret() 方法，直到递归深入到终结符表达式（如变量、常量）为止，最终返回结果


       (-)
      /   \
    (+)    c
   /   \
  a     b
